doctype html(lang='en')
head

  include ./pageheader.jade

  h4 The page for seller list data

    - for (var m = 0; m < #{merchLength}; m++)
      var currentMerch = merching[m];
        tr#merch_currentMerch._id
          td #{currentMerch.title}
          td #{currentMerch.price}
          td #{currentMerch.size}
          td #{currentMerch.endDate}
